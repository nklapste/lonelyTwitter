package ca.ualberta.cs.lonelytwitter;

import java.util.ArrayList;


/**
 * Subclass of {@code ArrayList} that implements a list of {@code Tweets}. This is not needed but
 * It helps in keeping the code readable and easy to maintain.
 *
 * No additional functionality or behavior is added to {@code TweetList} other than it only accepts
 * {@code Tweet}s.
 */
class TweetList extends ArrayList<Tweet> {
    // we don't need to implement anything!
}
